/**
 * Legacy JSON-file API removed.
 * Active entrypoint: `src/index.ts` (Postgres + Prisma + secure auth).
 */
console.error(
  '[dripless] src/server.js is retired. Use `npm run dev` (src/index.ts) instead.'
);
process.exit(1);
