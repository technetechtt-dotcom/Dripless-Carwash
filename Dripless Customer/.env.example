VITE_API_BASE_URL=http://localhost:4000
# Browser Maps JavaScript API key (HTTP referrer restricted). Enable Maps JS + Places.
VITE_GOOGLE_MAPS_API_KEY=
# Set true only for UI-only demos that bypass the remote API (local mock state).
# VITE_USE_MOCK_API=true
